import { SuiCheckboxDirective } from './sui-checkbox.directive';

describe('SuiCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new SuiCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
